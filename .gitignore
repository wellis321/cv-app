# PHP
/vendor
composer.lock
composer.phar
/storage/*
!/storage/.gitkeep
*.php~

# Environment variables
.env
.env.*
!.env.example
!.env.test

# macOS
.DS_Store

# Logs
logs
*.log

# Editor directories and files
.idea
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.suo

# Cursor configuration and cache
.cursor/

# Temporary files
Thumbs.db
*.tmp
*.bak
*~

# Account creation SQL files (may contain sensitive data)
database/create_account_*.sql

# Database exports (contain passwords, emails, PII – never commit)
database/data_export_for_hostinger.sql
database/complete_schema_for_hostinger.sql
database/user_cv_export_*.sql
backup.sql

# Export files (certifications/skills) – may contain user data; do not commit
scripts/exports/*.json

# SEO plan (internal – do not push)
docs/SEO_PLAN.md

# GSC export data (temporary – do not commit)
views/partials/*Performance*

# Ahrefs exports (temporary – do not commit)
*image-broken*.csv
*orphan*.csv
*meta-descriptio*.csv

# Old SvelteKit/Node.js files (if not deleted yet)
/node_modules
/.svelte-kit
/build
/output
/.vercel
/.netlify
/.wrangler
