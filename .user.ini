; PHP upload limits for job application files (PDF, Word, etc.)
; Used when PHP runs as FastCGI/CGI (some hosts ignore .htaccess php_value)
; Restart or wait ~5 min for changes to take effect
upload_max_filesize = 25M
post_max_size = 26M
